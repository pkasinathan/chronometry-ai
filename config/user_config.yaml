# Chronometry User Overrides
# ===========================
# Add your customizations here. Any key you set will override the
# corresponding default in system_config.yaml.
#
# Examples:
#   capture:
#     capture_interval_seconds: 600   # capture every 10 min instead of 15
#
#   annotation:
#     screenshot_analysis_prompt: |
#       Your custom prompt here...
#
#   notifications:
#     pre_capture_sound: true
#
# Run 'chrono config' to see the full merged configuration.
